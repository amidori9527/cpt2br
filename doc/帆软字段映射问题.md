+ **FineReport 数据列分组/列表/汇总**
+ CPT 里 `DSColumn` 的 `RG` 有：
    - `FunctionGrouper`
    - `SummaryGrouper`
+ `SummaryGrouper` 里有 `SumFunction / MaxFunction / NoneFunction`
+ 目标结构里是：
    - `dataSetting: "group" | "list" | "compute"`
    - `dataSettingDetail: 0|1|2|3|4`
+ 需要你确认：
    - `FunctionGrouper` 默认转 `group` 还是 `list`？
    - `SummaryGrouper + SumFunction` 是否转 `dataSetting="compute", dataSettingDetail=0`？
    - `MaxFunction` 是否转 `dataSettingDetail=1`？
    - `NoneFunction` 怎么转？
我的回答：1. FunctionGrouper默认转 group。2. 是。3. 是 4.转成dataSettingDetail=0
+ **FineReport **`**<Result>**`** 表达式**
+ 很多数据列有：
    - `$$$`
    - `=$$$`
    - `NVL($$$,'0.00')`
    - `"姓名："+$$$`
    - `CONCATENATE(...)`
+ 当前脚本忽略了 `Result`，只生成 `$DS.S(ds.field)`。
+ 需要你确认：
    - `Result` 是否应该转成 `type: "formula"`？
    - 还是保留 `type: "columnData"`，把表达式放到某个字段？
    - 如果 `Result=$$$` 才是普通数据列，其他都转公式？
我的回答：全部不转
+ **数据列过滤条件**
+ CPT 里 `DSColumn/Condition` 很多：
    - `ListCondition`
    - `CommonCondition`
    - `JoinCondition`
    - 比较对象有 `ColumnRow`、`SimpleDSColumn`
+ 目标结构里有 `meta[].filter[]`。
+ 需要你确认：
    - `CommonCondition` 是否转 `filter`？
    - `ColumnRow column="10" row="2"` 是否转 `refType="cell", refCell="2/10"`？
    - `SimpleDSColumn dsName columnName` 是否转 `refType="columnData"`？
我的回答：全部不转

+ **FineReport 扩展方向 **`**Expand dir**`
+ 样本里 `	"0"` 和 `dir="1"` 都很多。
+ 当前我把 `dir="1"` 转成 `expandDirection="v"`，`dir="2"` 转 `"h"`，但样本主要是 `dir="0"`。
+ 需要你确认：
    - FineReport `dir="0"` 在你的目标里应该是 `v`、`h`，还是 `none`？

我的回答：FineReport dir="0" 在我的程序中是 none

+ **父格**
+ CPT 里有：
    - `leftParentDefault="false" left="K3"`
    - `upParentDefault="false" up="B3"`
+ 目标结构里有：
    - `leftParentLattice`
    - `topParentLattice`
+ 当前我只处理了 `left/up`，没有处理 `leftParentDefault=true/false` 的默认语义。
+ 需要你确认：
    - 没有 `left/up` 但有 `leftParentDefault=false` 时，是 `none` 还是不写？
    - `leftParentDefault=true` 是否转 `"default"`？

我的回答：1. 是none。2. 是
+ **条件属性 **`**HighlightList**`
+ CPT 里大量 `HighlightList/Highlight/Condition/HighlightAction`
+ 目标是 `meta[].conditionAttribute[]`
+ 但 FineReport 的动作有 `RowHeightHighlightAction`、`ColWidthHighlightAction` 等。
+ 需要你确认：
    - 这些是否转成 `conditionAttribute.attributeActiveInfo`？
    - 条件表达式是否要转成 JS 脚本 `expressionStatement`？

我的回答：条件属性默认都先不处理


+ **字典显示 **`**Present DictPresent**`
+ CPT 里 `Present class="DictPresent"`，引用 `TableDataDictionary`。
+ 目标结构支持 `contentType: "dataDic"` 和 `dataDicOptions`。
+ 需要你确认：
    - FineReport 字典显示是否映射到 `contentType="dataDic"`？
    - `kiName / viName / TableData name` 分别对应 `dataDicOptions` 里的哪些字段？
+ **单元格分页**
+ CPT 有 `CellPageAttr pageBeforeRow="true"` 等。
+ 目标有：
    - `rowBeforePaging`
    - `rowAfterPaging`
    - `colBeforePaging`
    - `colAfterPaging`
+ 需要你确认 FineReport 属性到这四个字段的对应关系。

我的回答：字典默认都先不处理

+ **单元格自适应/显示属性**
+ CPT 有 `CellGUIAttr adjustmode="1" showAsDefault="true"`
+ 目标有：
    - `contentLayout`
    - `isAutoScaleContent`
    - `isHiddenContent`
    - `htmlContentShow`
+ 需要你确认 `adjustmode=1/0` 分别对应什么。
+ **子报表、图片、斜线文本、超链接、控件**
+ 样本中存在：
    - `O t="SubReport"`
    - `ImageWithSuffix`
    - `BiasTextPainter`
    - `NameJavaScriptGroup`
    - `Widget`
+ 目标结构都有类似字段，但语义不一定能无损转。
+ 需要你确认优先级：这些是第一版要保留，还是先忽略并记录 warning？

我的回答：默认都先不处理